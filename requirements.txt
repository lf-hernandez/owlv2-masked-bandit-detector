# Core ML dependencies
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
pillow>=10.0.0

# API dependencies (only needed if running the web API)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
